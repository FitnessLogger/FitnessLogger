import Foundation
import SwiftUI

struct TrainingProgramView: View {
    var body: some View {
        HStack {
            Button.init("Add Training program", action: {
                print("Button pressed")
            })
        }
    }
}
